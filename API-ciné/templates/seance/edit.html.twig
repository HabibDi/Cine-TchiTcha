{% extends 'base.html.twig' %}

{% block title %}Edit Séance{% endblock %}

{% block body %}
<div class="AdminBox">
    <h1>Edit Séance</h1>

    {{ include('séance/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('seance_index') }}">back to list</a>

    {{ include('séance/_delete_form.html.twig') }}
</div>
{% endblock %}
